@page "/tabs"

@using Controls.Tabs
@using Controls.Lists
@using Controls.Css
@using System.Drawing

@*<TabContainer Orientation="listOrientation" TabType="tabType">
    <Tab Colors="pillColors" IsDefault=true>
        <Content>
            First tab
        </Content>

        <ChildContent>
            <h4>Greetings from the first tab!</h4>
        </ChildContent>
    </Tab>

    <Tab Colors="pillColors">
        <Content>
            Second tab
        </Content>

        <ChildContent>
            <h4>Hello from the second tab!</h4>
        </ChildContent>
    </Tab>
</TabContainer>*@

@code {
    //private readonly ListOrientation listOrientation = ListOrientation.Horizontal;
    //private readonly TabType tabType = TabType.Pill;
    //private static readonly Styles defaultPillColors = new(new CssColor("background-color", Color.White), new CssColor("color", Color.FromArgb(0, 158, 247))),
    //    selectionPillColors = new(new CssColor("background-color", Color.FromArgb(0, 158, 247)), new CssColor("color", Color.White)),
    //    hoverPillColors = new(new CssColor("background-color", Color.White), new CssColor("color", Color.FromArgb(0, 109, 171)));
}