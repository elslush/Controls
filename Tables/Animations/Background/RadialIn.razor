@using Controls.Css
@implements IAnimation

<span>
    @ChildContent(classString)
</span>


@code {
    private static readonly Class classString = new("radial-in");

    [Parameter, EditorRequired]
    public RenderFragment<Class> ChildContent { get; set; }
}
