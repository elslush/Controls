@using Controls.Css
@implements IAnimation

<span>
    @ChildContent(classString)
</span>


@code {
    private static readonly Class classString = new("grow");

    [Parameter, EditorRequired]
    public RenderFragment<Class> ChildContent { get; set; }
}
