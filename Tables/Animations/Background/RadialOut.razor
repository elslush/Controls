@using Controls.Css
@implements IAnimation

<span>
    @ChildContent(classString)
</span>


@code {
    private static readonly Class classString = new("radial-out");

    [Parameter, EditorRequired]
    public RenderFragment<Class> ChildContent { get; set; }
}
