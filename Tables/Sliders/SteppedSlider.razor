<div class="stepped-slider-container">
  <input type="range" min="@MinValue" max="@MaxValue" step="@Step" @bind-value="BoundValue" class="stepped-slider">
  <div class="ticks">
        @for(int i = MinValue; i <= MaxValue; i += Step)
        {
            <span class="tick">@i</span>
        }
  </div>
</div>

@code {
    [Parameter]
    public int MinValue { get; set; } = 0;

    [Parameter]
    public int MaxValue { get; set; } = 100;

    [Parameter]
    public int Step { get; set; } = 1;

    [Parameter]
    public int SteppedValue { get; set; } = 0;

    [Parameter]
    public EventCallback<int> SteppedValueChanged { get; set; }

    private int BoundValue
    {
        get => SteppedValue;
        set => SteppedValueChanged.InvokeAsync(value);
    }
}
